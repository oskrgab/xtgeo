{
  "name": "xtgeo-wasm-smoke",
  "private": true,
  "type": "module",
  "description": "Node + Pyodide smoke test that loads the cross-compiled xtgeo wheel and asserts `import xtgeo` succeeds. The pyodide runtime is installed at the pinned version by run_smoke_test.sh (see versions.env)."
}
